@{
    ViewBag.Title = "Регистър медиатори";
}
<v-app>
    <searchmediator inline-template v-bind:for-mediation="false">
        <div id="searchmediator">
            <template v-if="loading">
                <v-text-field loading
                              label="Зареждане..."
                              disabled>
                </v-text-field>
            </template>
            <template v-else>
                <v-container>
                    <v-row>
                        <v-col cols="12">
                            <v-sheet elevation="3">
                                <v-tabs align-with-title :show-arrows="true">
                                    <v-tabs-slider color="primary"></v-tabs-slider>
                                    <v-tab ripple centered class="black--text">
                                        Основна информация
                                    </v-tab>
                                    <v-tab ripple centered class="black--text">
                                        Контакти
                                    </v-tab>
                                    <v-tab ripple centered class="black--text">
                                        Образование
                                    </v-tab>

                                    @* By General Information *@
                                    <v-tab-item>
                                        @Html.Partial("_SearchByDefaultView")
                                    </v-tab-item>
                                    @* By Contacts *@
                                    <v-tab-item>
                                        @Html.Partial("_SearchByContactsView")
                                    </v-tab-item>
                                    @* By Education *@
                                    <v-tab-item>
                                        @Html.Partial("_SearchByEducationView")
                                    </v-tab-item>
                                </v-tabs>
                            </v-sheet>
                        </v-col>
                    </v-row>
                    <template>
                        <v-row>
                            <v-col cols="12">
                                @Html.Partial("_SearchResultDataTableView")
                            </v-col>
                        </v-row>
                    </template>
                </v-container>
            </template>
        </div>
    </searchmediator>
</v-app>

@section Scripts {

    @* Models *@
    @Scripts.Render("~/JSModels/FormFilingClasses/FullAddress.js")
    @Scripts.Render("~/JSModels/FormFilingClasses/Contact.js")
    @Scripts.Render("~/JSModels/FormFilingClasses/Education.js")
    @Scripts.Render("~/JSModels/SearchMediators/MediatorFilters.js")

    @* Components *@
    @Scripts.Render("~/Scripts/Custom/load-nomenclatures.js")
    @Scripts.Render("~/Scripts/VueComponents/searchMediator.js")
}